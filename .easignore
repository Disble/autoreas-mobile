.expo/
.jest/
coverage/
.vscode/
.claude/
.atl/
.agents/
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Managed project: native folders are generated by EAS's own prebuild. A host `npx expo prebuild`
# output must never reach the build, and neither must previous local APKs.
/android/
/ios/
build-*.apk
/dist/
