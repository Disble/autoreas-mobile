/** Provides the shared app root layout default label value. */
const _AppRootLayoutDefaultLabel = 'AppRootLayout';

/** Provides the startup failure fallback title. */
export const APP_ROOT_LAYOUT_STARTUP_FAILURE_TITLE = 'No pudimos iniciar la app';

/** Provides the startup failure fallback summary. */
export const APP_ROOT_LAYOUT_STARTUP_FAILURE_DESCRIPTION =
  'La app encontró un problema local antes de abrir tus pantallas.';

/** Provides the safe diagnostic section title. */
export const APP_ROOT_LAYOUT_STARTUP_FAILURE_DIAGNOSTIC_TITLE = 'Diagnóstico seguro';

/** Provides the recovery section title. */
export const APP_ROOT_LAYOUT_STARTUP_FAILURE_RECOVERY_TITLE = 'Qué hacer ahora';
