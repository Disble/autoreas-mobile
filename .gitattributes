# Shell scripts run inside Linux containers (see docker/eas-build-entrypoint.sh). Force LF so a
# Windows checkout with a different core.autocrlf setting can never introduce CRLF, which breaks
# the shebang and every `#!/usr/bin/env bash` invocation inside the container.
*.sh text eol=lf
