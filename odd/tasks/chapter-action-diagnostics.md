# Chapter action diagnostics

## Objective and problem
Make the four chapter-control callbacks (`capPlus`, `capMinus`, `capPlusHalf`, `capMinusHalf`) diagnosable with a few question-driven observations delivered through **only** `POST /api/sync/diagnostics`. Today mutation errors enter a volatile JS ring, foreground sync does not capture it, JS can duplicate cycle telemetry between diagnostics and reconcile, and native FGS does not flush the diagnostics outbox. A JS callback is not proof of a physical tap; disabled buttons and a non-running JS thread cannot be observed at this boundary.

## Scope and constraints
- Branch/worktree: `feat/chapter-action-diagnostics` at `../autoreas-mobile-chapter-telemetry`, isolated from the dirty `test/sync-core-assurance` worktree.
- Capture only events answering: did an enabled-button JS callback run; was it dropped/no-op; did the local write commit/fail; did the immediate post-write sync succeed/fail. Keep a bounded, allowlisted action/outcome vocabulary and correlation sufficient to distinguish a callback from its outcome. Do not send anime IDs, titles, patches, SQL, exception messages or arbitrary strings.
- Persist each observation AT EVENT TIME into the existing durable diagnostics outbox (`createSyncDiagnosticsOutboxStore().enqueue`), whose own connection to `autoreas-telemetry.db` is deliberately outside `withLocalWrite`; do not route chapter observations through the volatile in-memory ring nor wait for a later headless cycle.
- Keep one compact, extensible event vocabulary (one `chapter_action` event with a closed `phase` plus optional closed `outcome`/`reason`/`duration_ms`/`correlation_id`), so future diagnostics add vocabulary values only. Kotlin transports stored JSON opaquely via the existing HTTP transport and must not learn new event vocabulary. Remove duplicate `client_telemetry` from JS/native reconcile requests. Preserve pending operation reconciliation and keep diagnostics best-effort.
- Do not include anime identity in the payload. Two sequential actions must remain distinguishable; unique outbox ids per observation, correlation id shared across one action's observations.
- Bridge acceptance of the additive envelope is not verifiable from local mobile code; confirm before relying on it.
- Avoid edits to historical OpenSpec/docs artifacts and the other session's work. Do not commit without explicit user confirmation after validation and diff review.
- Known limitation: no native physical-touch tracing; no JS event exists if JS callback does not execute.

## Test mode and agreed seams
- Repository AGENTS.md requires RED → GREEN → MUTATE → REFACTOR when changing hooks/helpers; Jest tests under tests/ and Kotlin test harness `npm run test:kotlin`. Focused Jest runner: `npx jest --runInBand <focused paths>`.
- Proposed behavior seams, pending user confirmation before test writing: chapter callback/guard and mutation result as seen through feature public interfaces; durable capture and BridgeClient-backed diagnostics send; native background cycle transport as seen through its test HTTP server. Avoid internal collaborator call-count tests.

## Tasks (stable IDs)
- [x] **C1** (delegated direct writer; multi-file): establish compact, privacy-safe chapter observations at JS callback/mutation boundaries, persist to diagnostics outbox and deliver through the existing JS diagnostics endpoint without altering user mutation outcomes. DONE and independently verified — see Status below.
- [ ] **C2** (delegated direct writer; multi-file): REWRITTEN after the bridge contract review below. Land the agreed wire shape (discriminated envelope, `kind` always present, `episode_*` tokens, `observation_id`, `observed_at_ms`, cross-field `outcome`/`phase`) once the bridge confirms the final field set, add `episode_action` to `SYNC_DIAGNOSTICS_ACCEPTED_KINDS`, and rebuild the payload builder against it. Then the native courier: let the Kotlin background cycle flush queued diagnostic JSON through its existing HTTP transport, and remove the duplicate `client_telemetry` from both the JS and Kotlin reconcile requests LAST — the Kotlin copy is not a duplicate today, it is the only telemetry the native path emits. RED/GREEN/MUTATE and focused Jest/Kotlin checks.
- [ ] **C3** (parent verification): review diff, run relevant focused and full gates (`npx lefthook run pre-commit`; `npx -y react-doctor@latest . --verbose --diff` if React code changes; Kotlin tests where applicable), assess skipped/failing checks and delivery behavior. No commit until explicit user confirmation.

## Acceptance
- Bridge receives chapter observations only through the diagnostics endpoint, with a durable path after callback receipt and no anime-specific payload; foreground JS or native FGS can flush pending envelopes without altering sync success/failure.
- The four questions are individually answerable or explicitly reported as unobservable; two sequential actions must not accidentally coalesce across distinct outcomes.
- The duplicate `client_telemetry` is removed from `/api/sync/reconcile` on both sides ONLY once the dedicated path actually carries the data. Removing it earlier would zero out the native path's only telemetry and leave episode observations still rejected.

## Outbox delivery policy (decided, agreed with the bridge team)

This is a CHOSEN policy with stated costs, not an emergent property of local fixes. It was settled across a cross-repo review with the bridge maintainers, who verified each claim against their handler and store.

**Eviction sheds the tail, not the head.** *(IMPLEMENTED in this work unit: `SYNC_DIAGNOSTICS_OUTBOX_EVICT_TRIGGER_SQL` sheds `ORDER BY created_at DESC, rowid DESC`, the trigger records every shed row in the additive `sync_diagnostics_outbox_shed_count` table in the same statement, and `SyncDiagnosticsOutboxStore.getShedCount()` reads it. The retained-prefix invariant is pinned by the ordering tests in `tests/infrastructure/db/sync-diagnostics-outbox.helpers.test.ts`.)* The cap trigger deletes the NEWEST overflow rather than the oldest, because the candidate query reads oldest-first: under the old ordering an insert could evict rows a drainer had just read, and a failing POST then lost them silently, making the bridge's own "a delivery failure never silently loses it" promise false in exactly the case it was written for. With tail-shedding the retained prefix is always the oldest rows, so a row in flight is unreachable by eviction and can only be removed by the drainer that owns it — no claim, no lease, no per-row state, no migration. **Load-bearing invariant, pinned by test**: the guarantee holds only while the candidate query's order matches the retained-prefix order; a filtered or reordered read breaks it silently with nothing to catch it, so the test asserts that the survivors ARE the ordered prefix a candidate read returns and that a row already read as a candidate is never among the shed.

**Classification by recoverability, three ways, on the payload's top-level `kind`** (that one field is read; everything else stays opaque and the body is posted byte-identical):

| Case | Action | Why |
|---|---|---|
| `kind` absent, or in `SYNC_DIAGNOSTICS_ACCEPTED_KINDS` | ROUTABLE — POST; delete only on a definitive verdict | deliverable |
| `kind` in `SYNC_DIAGNOSTICS_UNDELIVERABLE_KINDS` (declared; **empty today**) | never post; DELETE on sight; count; batch continues | this build KNOWS the bridge does not accept it, so parking could never be resolved by anything we can do — the stall would be permanent |
| `kind` in neither set | never post; **NEVER delete**; count separately; batch continues | it belongs to a DIFFERENT build of our own app (an app rollback does exactly this), so rolling forward recovers it — the stall is contingent on an operator action, not permanent |

The cut is recoverability, not familiarity. An earlier revision destroyed anything not currently in the registry, which made a normal app rollback destroy a whole kind's backlog in one pass and turned a recoverable registry mistake into an irreversible one. **Destruction now requires a positive declaration**; "not currently in the registry" is no longer a destruction trigger.

**No state is unresolvable.** A kind no build will ever accept — a bug, or a build that is long gone — parks forever with no roll-forward to rescue it, and is recovered through the same door: an operator moves it into `SYNC_DIAGNOSTICS_UNDELIVERABLE_KINDS`. That is why the positive set can legitimately be empty and still complete.

**Verdict set — the other half of the destruction authority, defined by the bridge's contract, not by our registry.** The bridge's authoritative declaration: the contract for `POST /api/sync/diagnostics` has exactly ONE permanence property, and **permanent-for-the-body is exactly `400` and `413`**. `isEnvelopeRejection` is being corrected from `400 | 413 | 422` to `400 | 413`:

| Status | Meaning |
|---|---|
| `204` | accepted in both branches (stored or duplicate) — always safe to delete on |
| `400` both shapes, `413` | DEFINITIVE — permanent for that body, destruction justified |
| `405` | not a body verdict: reachable only for a non-POST method, so seeing it means a routing bug, not a rejection of our bytes |
| `404`, `408`, `422`, `429` | NOT in the contract at all — and anything the contract does not declare must be treated as retryable |
| `401`, `404`, `408`, `429`, `500`, `503`, transport failure | NOT definitive — retry, never destroy |

**The general rule that makes the specific list safe to remember: permanence is declared by the bridge's contract and by nothing else, so anything that contract does not declare is retryable.** `401` is a credential problem, never a body problem, so destroying on it would be data loss on the first token rotation. `405` is a routing bug if a client that always POSTs ever sees it. **`422` was removed**: it was in this set by inheritance from the bridge's `season_rating_handler.go`, a different endpoint whose answer about a grade says nothing about whether the diagnostics endpoint will reject the same bytes forever — inheriting it is the same error the vocabulary rules already forbid for shared sets across positions, and it was an undeclared destructive assumption, which the classification above had just committed us to avoid. Re-adding it requires the bridge to declare a permanent `422` for this endpoint. Note also that the bridge's backpressure is `503` with `Retry-After: 5`, not `429`.

**Counters — three, distinct, never conflated**: `undeliverable` (destroyed by judgement), `unclassified` (parked and unknown to this build), `shed for capacity` (dropped at the door by the cap). Recorded at the point of the event — and the cap's own counter is recorded by the eviction trigger itself, in the transaction that drops the row, then read as a cumulative total through `getShedCount()`. It is deliberately a STORE fact rather than a field on the flush result: an INSERT can hit the cap outside any flush pass, so a flush-shaped counter would report the loss late or not at all.

**Costs, stated so they are not hidden transfers:**
- A sustained rollback with a saturated queue of unclassified rows consumes delivery capacity and stalls the deliverable rows until roll-forward. That is the deliberate price of not destroying another build's data; it is bounded by an operator action, not permanent.
- Tail-shedding trades freshness for durability: after a long outage the retained telemetry is the ONSET of the problem rather than its current state, while the surface that reads it shows current sync state. This is product-visible and was surfaced as a policy question rather than presented as an internal detail.
- **Negative-schema items**: we have no client-side body-size guard, so an oversize body is learned about only as a vanished row plus a discard counter. Latent because our ring cap of 20 cannot reach the bridge's 8 KiB bound; the headroom is on the bridge's side, since their server accepts up to `maxRecentEvents = 32`. **The latent item becomes live the day the client ring is raised toward that limit**, so the two numbers belong coupled in one comment next to the ring constant.
- **Declaration, not yet an artifact**: the bridge declared the verdict set authoritatively in a coordination message so we could stop a destructive predicate, and explicitly could not land it as an artifact yet — it belongs in their `docs/openapi.yaml` plus a dated API-consumer-impact entry, both in WU1 scope. Until it lands, that message is the declaration. The shipped-versus-designed distinction applies to contracts too, one layer down.

**Flip-round trigger (agreed)**: `skipped`, `undeliverable` and `unclassified` must all become visible when the registry is flipped. If any is still invisible at that point it stops being a recorded gap and becomes a live one — with extra force for `undeliverable`, because it is a DESTRUCTION counter rather than a gap counter.

## Status/evidence/next step
- **C1: complete, independently verified, committed.** Recorder plus mutation-path wiring emit `received` / `skipped(in_flight|anime_missing|db_unavailable)` / `finished(committed|failed, closed cause, duration_ms)` / `sync(ok|failed)` into the durable outbox, one `correlation_id` per tap. Two review slices: `65efc10` (recorder + vocabulary + unit tests) and `a69e0b3` (wiring + tests).
- **Verification of record (independent agent, different model from the writer).** Eight adversarial claims checked; the privacy boundary, single-clock semantics, correlation uniqueness, caller safety, best-effort guarantee and test honesty all held. Two real defects were found and fixed before staging: the test file broke `npx tsc --noEmit` with two `TS2345` errors (the casts indexed optional observation members, so they included `undefined`) — this would have failed the pre-commit typecheck while Jest stayed green; and the missing-SQLite-context path threw before the `try`, so a tap emitted only `received` with no terminal phase while a comment claimed the opposite. That path now reports `skipped`/`db_unavailable`, a new closed vocabulary value: the write never ran, so `finished`/`failed` would overstate what the device knows.
- **The bridge rejects this payload, and the flush deleted it — fixed in `8a1a2b1`.** The counterpart repo strict-decodes `POST /api/sync/diagnostics` with `DisallowUnknownFields()`, so the payload's undeclared top-level `kind` is answered with 400, and this flush treats 400/413/422 as a permanent rejection and removes the stored row. Every observation posted was destroyed client-side and counted as `discarded`. Two gates now prevent it: the user-owned telemetry switch is honoured in BOTH places it was ignored (the recorder and the drain), and a single closed registry `SYNC_DIAGNOSTICS_ACCEPTED_KINDS` admits only the legacy kindless cycle envelope, so nothing unpostable is produced and no pre-existing unroutable row is re-posted. Parking those rows was rejected: the outbox drains oldest-first in batches of three with no per-row state, so unroutable rows would starve the deliverable cycle envelopes behind them.
- **Consequence accepted, stated plainly.** With the registry as it stands, this branch captures NOTHING for these gestures: no observation is produced until the bridge declares the kind. A visible absence was chosen over a silent delete. Adding `episode_action` to that registry is the single change that lights the feature up.
- **Bridge contract agreed (supersedes the transport plan in this document).** The bridge withdrew its own sibling-path proposal and is building a discriminated envelope on the existing path: one telemetry store, split by `kind`, with a per-kind retention budget. Agreed: our variant ALWAYS carries `kind`; absence of `kind` is a frozen compatibility rule for the already-deployed cycle report only. Wire names: `episode_action`, `episode_plus_one` / `episode_minus_one` / `episode_plus_half` / `episode_minus_half`, `observation_id` as the idempotency key, `observed_at_ms`, `phase` and `correlation_id` unchanged, `duration_ms` always present and `null` when not applicable (non-null on `sync` rejected), `outcome` gated as ONE cross-field rule against `phase`. `chapter` is retired vocabulary on the bridge side (`docs/ubiquitous-language.md`); mobile keeps `ChapterAction*` internally and translates once at the boundary. **The field set is now FINAL, declared by the bridge and verified by the parent against their binding artifact commit `5bbcbf3` on `feat/diagnostics-kind-store`** — the kind-discriminated store with `UNIQUE (kind, event_id)`, the kind registry and `cycle_report` delegating to the unchanged validator are decoder-backed and committed; the `episode_action` variant is a declaration only, not yet registered, validated or reachable. Two clauses are stricter than the earlier summary: `duration_ms` is always present with `null` where it does not apply and a non-null value is accepted only on `phase: finished`, and `cause` is accepted only on `finished` with `outcome: failed`. The body is strict-decoded with `DisallowUnknownFields` on their side. The builder is written to that contract and the mobile emission stays gated by `SYNC_DIAGNOSTICS_ACCEPTED_KINDS`.
- **The flip is effectively PERMANENT for that token.** The bridge states that once a value is stored, changing it means reshaping rows, which they will not do inside their lifecycle. So the flip happens only against their confirmation that the endpoint accepts the kind — meaning registered, its validator committed, its `400` behaviour tested, and the endpoint dispatching it, verified against running code. Never on prose, never on an assumption, and never on a partial confirmation. **Governing rule for this channel: the repository artifact governs, never a session's prose** — if any bridge session contradicts `5bbcbf3` or `odd/tasks/diagnostics-kind-store.md`, escalate that claim rather than averaging the two.
- **Accepted risk, no code change.** A theoretical double-`finished` window exists between the commit record and the sync chain start in `anime-mutation.helpers.ts` if `beginSyncConnectionAttempt()` ever threw synchronously. It is unreachable in practice, since `syncPendingOperations` is an async function and cannot throw synchronously. No speculative guard was added.
- **Concurrency: resolved, no protection needed.** Two drainers could post the same row twice, and `remove()` reports `removed` without inspecting affected rows — but the bridge is idempotent by `cycle_id` (`ON CONFLICT DO NOTHING`, still acks 204, "mobile can retry blind and stay correct"). The receiver already implements idempotent consumption, so no claim, lease, per-row state machine or schema migration is warranted. A per-row claim design was proposed and withdrawn as over-engineering.
- **Mobile observability gap, found and not yet fixed.** `sync_runtime_status.last_diagnostics_discarded_count` is persisted only by the headless path; the foreground coordinated cycle drops `result.diagnosticsFlush`. On a native-engine build the column may never update from the path that is actually discarding rows.
- **Pre-existing debt paid in passing.** `tests/features/animes/__tests__/use-anime-list-screen.test.ts` was 613 lines at HEAD, already over the 500-line limit, so the staged-file lint job could never pass on it. It was split into a shared harness plus two test files with an identical 28-case title multiset, and its 13 JSDoc findings resolved. `tests/features/animes/use-mutate-anime.test.ts` remains at 517 lines with 12 findings: untouched, so it stays out of the diff, and it will fail lint if it is ever staged.
- **Outbox cap landed as tail-shedding, with the cap's loss counted and the read-order invariant pinned.** `SYNC_DIAGNOSTICS_OUTBOX_EVICT_TRIGGER_SQL` now sheds `created_at DESC, rowid DESC` (was `ASC, ASC`), so the retained window is the same oldest prefix `SYNC_DIAGNOSTICS_OUTBOX_SELECT_CANDIDATES_SQL` returns and a row a drainer has already read is unreachable by eviction. The shed count is recorded by the trigger in a new additive `sync_diagnostics_outbox_shed_count` table (`IF NOT EXISTS`, never a column added to an existing table: this DDL re-runs on every connect and there is no migration path, so only an idempotent shape can provision an installed device) and exposed as `SyncDiagnosticsOutboxStore.getShedCount()`, mirroring `getFailedWriteCount`; `changes()` records the rows the `DELETE` actually removed, never the overflow the `WHEN` clause predicted. One EXISTING assertion was corrected, not weakened: the eviction test asserted the old contract (`cycle-0` shed, the overflow row retained) and now asserts the inverse. A direct unit test for the pure ladder in `sync-diagnostics-disposition.helpers.ts` was added in the same pass (`tests/features/sync/__tests__/sync-diagnostics-disposition.helpers.test.ts`), closing the gap left when that module was extracted with the focused suite at its line ceiling.
- **Native courier: implemented and aligned.** The Kotlin side now drains the outbox through its existing HTTP transport, posting each stored body byte-identically and opaquely, with the same verdict set (`400`/`413` only), the same header-less-`503` deferral scoped to `503`, the same batch size and a bounded drain budget, and it gates on the user's telemetry switch — which the native side previously did not read at all. Its three-way classification now matches the JS drainer exactly, including the two edge cases: absence of the `kind` KEY is the only legacy signal, and both a present JSON-`null` kind and a body that does not parse as a JSON object park rather than route, because routing them meant `400` → definitive → the row deleted. Two drainers disagreeing about what is routable or destructive was the hazard this alignment closes.
- **Next**: flip `SYNC_DIAGNOSTICS_ACCEPTED_KINDS` and cut over to the dedicated diagnostics path — but only against the bridge's confirmation that the endpoint accepts the kind (registered, validator committed, its `400` behaviour tested, dispatching, verified against running code). The payload builder is already written to the frozen contract, so the flip itself is one line. Do not write mobile code against unconfirmed wire names.
- **Open item, precisely: the counters are persisted but NOT observable where they are produced.** `recordBacklogReadCount` is called only from `headless-sync-cycle.helpers.ts`, and the foreground coordinated cycle consumes `result.syncedCount` while dropping `result.diagnosticsFlush`. On a build where the native engine owns background, the headless JS cycle no-ops, so `lastDiagnosticsDiscardedCount`, `lastDiagnosticsUndeliverableCount` and `lastDiagnosticsUnclassifiedCount` are never written by the path that performs the discarding — which for `undeliverable` means an unmeasured destruction, the very objection the policy answers. Two candidate fixes, neither free: mirror the headless call in the foreground path, which adds one status write per cycle because that path does not already do a bookkeeping write; or fold the counters into the attempt-succeeded write that path already performs, which preserves "zero new transactions" but must handle the early return taken when `hasMorePending` is true. Not squeezed in at the end of a long round on a hot path; it is a decision worth its own round.
- **Resolved: the fold was chosen, and the mirror was rejected on evidence, not on taste.** The foreground coordinated cycle now passes `result.diagnosticsFlush` to the attempt-succeeded write it already performs (`recordSyncAttemptSucceeded` gained an optional trailing `diagnosticsFlush`), so `lastDiagnosticsDiscardedCount`, `lastDiagnosticsUndeliverableCount` and `lastDiagnosticsUnclassifiedCount` are persisted from the path that performs the discarding at **zero new status transactions** — the mirror's `+1` write per cycle was avoided by construction. The three counters are mapped by ONE shared pure function (`buildDiagnosticsCounterPatchFields`) used by both writes that carry them, so the headless path (`recordBacklogReadCount`) and this one cannot drift apart; `discarded`, `undeliverable` and `unclassified` keep their exact meanings.
- **Why the mirror was NOT taken, precisely.** (1) `recordBacklogReadCount` requires the outbox `getFailedWriteCount()` and `readOperationLogConvergence(rawDb)`; the latter reads `rawDb.getFirstAsync`, and `tests/features/animes/anime-mutation-connection.test.ts` drives `runCoordinatedForegroundSyncCycle` with `rawDb = { name: 'raw-db' }` (lines 249/299) — a direct mirror fails there, and that file is outside this round's edit surfaces. (2) Mirroring writes the whole convergence projection and the backlog-read/failed-write counts from the foreground too, roughly six extra queries per pull and eight extra columns, far beyond the three counters that were being dropped. (3) The mirror placed inside the stale-guarded publication would give up `hasMorePending` coverage anyway, keeping every cost and losing the only benefit.
- **Deliberate consequence, pinned by tests.** The counters are written only where the foreground path writes status at all: a cycle that returns early on `hasMorePending` and a cycle that throws after reconcile (the failure patch carries no flush evidence) leave the columns at the previous cycle's measurement. Neither fabricates a zero, which is what Decision 7 forbids. Both paths are pinned so adding a write there is a conscious contract change: `never writes status for the hasMorePending early return, so its flush counters stay unpersisted` in `tests/features/sync/__tests__/sync-facade.helpers.test.ts`, plus the pre-existing exact-equality assertion `buildSyncAttemptSucceededPatch persiste success y syncedCount observable` (line 107), which fails if the fold ever invents counter keys for a caller with no flush evidence.
- **Evidence for this round**: RED then GREEN on `hands the cycle flush result to the attempt-succeeded write it already performs` (RED showed the call arriving with four arguments); MUTATE removed `result.diagnosticsFlush` at the call site, the assertion failed naming the missing flush object, the exact text was restored and the suite went green again; `npx jest --runInBand` = 187 suites / 1504 tests passed, `npx tsc --noEmit` = 0, `npx eslint --max-warnings=0 --no-warn-ignored` on the four touched files = 0, `bunx fallow audit` = verdict pass, `complexity_introduced` 0 and `duplication_introduced` 0. **Not covered**: no real-row test drives the new optional argument through a migrated adapter — `tests/features/sync/__tests__/sync-runtime-status.helpers.test.ts` sits at 499/500 lines, so the column-level proof rides the existing real-adapter terminal tests in that same file plus the boundary assertion above.
- **Also still open**: the store-level shed count (`getShedCount()`) is readable but has no column, no consumer and no surface, deliberately left as a fact of the outbox store rather than of one drain pass. The `shed for capacity` counter is now readable from the store, but NO surface consumes it yet: `sync_runtime_status` has no shed column and the flip-round trigger below requires it visible when the registry is flipped.
