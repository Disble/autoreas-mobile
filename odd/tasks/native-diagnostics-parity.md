# Native diagnostics parity

## Objective
Finish the highest-priority mobile follow-ups to the live `episode_action` flip without dropping native cycle telemetry. This is a follow-up to `odd/tasks/chapter-action-diagnostics.md` on branch `test/sync-core-assurance` (working tree clean at `cacf12d` before this round).

## Evidence and constraints
- JS accepts `episode_action`, Kotlin's native courier uses an empty accepted-kinds registry. Background-native delivery therefore parks episode rows and the age bound can reap them after seven days.
- The bridge v1.15.0 decoder serves `episode_action`. The user confirmed their bridge runs v1.15.0+. Keep the legacy absent-kind case and positive-only destruction registry unchanged.
- Kotlin currently has no native cycle-report outbox producer; deleting `client_telemetry` from its reconcile request loses native cycle telemetry. Do not remove it without an equivalent dedicated-path producer and contract proof.
- Strict TDD from repository AGENTS.md: RED → GREEN → MUTATE → REFACTOR; focused Kotlin/Jest then `npx lefthook run pre-commit`. Do not install the app. Commit only after explicit user confirmation per repository AGENTS.md and harness safety.
- Forecast: ~250-450 authored lines across tests, native parity and status visibility; delivery strategy `single-pr` locally, no PR/push authorized. No parallel writes in this worktree.

## Tasks
- [x] **N1 — native kind parity** (delegated writer; Kotlin registry + corresponding tests): accept the literal `episode_action` on the native drainer; retain unknown-kind parking, legacy kindless delivery and rejection behavior. Prove RED, GREEN, MUTATE and focused Kotlin tests.
- [x] **N2a — persisted loss visibility** (delegated writer; Settings helper and tests): surface the existing `undeliverable`, `unclassified`, `reaped` snapshot counters without fabricating zeros. Prove focused tests.
- [x] **N2b — capacity shed visibility** (delegated mapping and writer): expose the existing durable cumulative `getShedCount()` through an honest status/settings path without inventing per-cycle measurements or extra writes; determine the smallest bounded edit surface first. Prove focused tests and full gate.
- [x] **N3 — cutover decision** (parent): establish whether an equivalent native `cycle_report` producer and bridge contract can be implemented safely. Keep Kotlin reconcile telemetry until then. Report this as pending rather than claiming cutover complete.
- [x] **N4 — work-unit commit** (parent): the maintainer confirmed the commit after review. Work unit `c13693dd337e1224940bcebf69122e26def454c2` (`fix(sync): deliver native episode diagnostics and expose loss`) contains the behavior, tests and feature record; the installed pre-commit hook passed. Push/merge/release remain separate decisions.

## Acceptance
A native background drain posts an `episode_action` row instead of parking it; a still-unknown kind remains parked. Diagnostic loss is observable on the mobile status surface. No native cycle telemetry is silently removed. Parent reports passing, failed, skipped and pending gates; no build installation, merge, push or release.

## Progress
- 2026-09-25: Read-only map established native empty registry and absent native cycle producer.
- N1 implemented (uncommitted): Kotlin registry now accepts `episode_action`; a default-registry sibling test proves the live path posts it verbatim, while `watch_session` still parks and kindless envelopes still route. Focused RED 2 failures/24 tests, GREEN pass; mutation back to emptySet failed 2/4 tests, restored. `bun run test:kotlin` BUILD SUCCESSFUL including Kover; existing 500-line courier test stayed at 500. Worker did not stage for mutation because its contract forbids staging; behavior was mutated/restored in place. Parent `git diff --check` passed. Full pre-commit gate and independent verification pending; no commit yet.
- N2a implemented (uncommitted): Settings tiles now distinguish positive-declaration destruction (`undeliverable`), recoverable parked unknown (`unclassified`) and age-bound reap (`reaped`), with null omitted and measured zero shown. RED focused Jest 2 failures/8 passes, GREEN 10/10; two label mutations failed their guards and were restored. `npx tsc --noEmit` and targeted ESLint pass. Settings helper 417 lines; test 469. Parent `git diff --check` passed.
- N2b implemented (uncommitted): the cumulative shed count is read directly from its own telemetry store in the Settings hook (not copied to runtime status or written per cycle). New `readShedCount()` distinguishes unreadable (`null`) from readable empty (`0`), retaining the original `getShedCount()` compatibility contract. The first read can initialize schema, so after independent review found that render-time reading performed DDL, it was moved into a deferred effect. A content-stamped result prevents stale values and avoids an identity-triggered render loop. Refresh is only on mount/sync-status content changes, **not real time**; null/pending/unavailable omits the tile. RED 10 new failures on first attempt; five further focused RED cases caught render-time read and stale/cancelled results. GREEN 14/14 in the post-correction focused suites, 67/67 broader Settings tests; mutation guards failed and were restored (post-read cancellation is defensive and untestable while read is synchronous). The independent verifier confirmed no render-time DDL. `npx lefthook run pre-commit` passed: 193 suites/1675 Jest tests, full lint/fallow/typecheck/Kover; Kotlin tests were UP-TO-DATE in this final gate, but worker's N1 focused run executed 24/24 and the cached full run reports 260/260 since the latest Kotlin write. `test:mutation:staged` ran but mutated nothing (files outside its 3 surfaces). Standalone React Doctor diff exits 1 on **one pre-existing finding in unmodified** `src/features/animes/ui/AnimeListScreen/use-anime-list-screen.ts:237`; no new finding in staged files. `git diff --cached --check` passed. No device/bridge run, build, installation, commit, push or merge.
- N3 decision: the bridge v1.15 contract treats `client_telemetry` on reconcile as optional/ignored, so JS could remove its duplicate safely. Kotlin has **no** native `cycle_report` outbox producer: its reconcile envelope is its only native cycle telemetry. Do **not** remove Kotlin `client_telemetry`, or perform a partial JS/Kotlin cutover, until an equivalent native producer is specified, implemented and verified on the dedicated endpoint. This is a separate follow-up, not a harmless deletion; no such removal was made.
- N4: user explicitly confirmed the commit; `c13693dd337e1224940bcebf69122e26def454c2` landed on `test/sync-core-assurance` with the installed pre-commit hook green (193 suites / 1675 Jest tests, native/Kover, lint, typecheck, fallow). Actual authored diff was 676 additions and 32 deletions across 13 files, above the initial forecast; the chosen `single-pr` strategy remains a review-workload risk rather than an authorization to open a PR. The present record-only follow-up captures that commit identity after Git assigned it.
