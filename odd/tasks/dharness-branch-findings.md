# Dharness branch findings

## Objective
Remove exactly the three branch-scoped static-analysis findings on `test/sync-core-assurance` (starting at `a82b542`): React Doctor's compiler bailout in `use-anime-list-screen.ts:237`, plus Fallow clone groups `dup:0bfbb1bf` (Settings count tiles) and `dup:d322e06e` (sync status/telemetry type declarations). No repo-wide lint/Doctor debt cleanup, config suppression, or changes to machine-owned `.dharness/` files.

## Evidence and constraints
- `npx -y react-doctor@latest . --verbose --diff` (0.9.14, rebases against `origin/main`) exits 1 with the single `react-hooks-js/todo` bailout at the chapter mutation `try/finally`. The same hook also has a refresh `try/finally`; avoid merely relocating the same error.
- `bunx fallow audit --base dev --format json --quiet --explain` (3.4.2) reports exactly two introduced duplicate groups. Group 0bfbb1bf is three consecutive runs of three `pushCountTile` calls in Settings; group d322e06e spans required/optional status counters and camelCase/wire telemetry declarations. Pre-existing complexity findings are inherited, not part of the three.
- `dharness check` inspects staged source only and reports `no staged source files` on a clean tree; this is not a passed analysis. Stage exact reviewed files before using it for verification.
- Existing public seams: AnimeListScreen chapter/refresh callbacks and Settings section tile output; type-only changes use the existing sync contract tests plus TypeScript. Preserve synchronous mutation lock before the first await, telemetry before the guard, rejection/toast safety, refresh state reset, tile order/null-vs-zero/tones, and wire shape. Do not widen or infer closed wire vocabulary.
- Strict TDD from repository AGENTS.md: RED → GREEN → MUTATE → REFACTOR for changed helpers/hooks; focused `npx jest --runInBand <paths>`, `npx tsc --noEmit`, `npx lefthook run pre-commit`. React changes also require `npx -y react-doctor@latest . --verbose --diff`. No source write before this record/mirror/todo.
- No commit without fresh explicit user confirmation after validation and diff review. No push/PR/merge. Initial forecast ~300-400 authored changed lines; actual staged candidate is 774 additions + 210 deletions across 11 files. Review in three work units (one per finding) rather than as a single oversized commit; no PR/chaining decision has been authorized. One writer at a time.

## Tasks
- [x] **D1 — compiler-safe AnimeListScreen callbacks** (delegated writer): remove the reported `try/finally` from hook execution without changing chapter action lock, telemetry or refresh outcomes; prove failing behavior/diagnostic before implementation, focused Jest and React Doctor after.
- [x] **D2 — Settings count tile duplication** (delegated writer): replace nine structurally identical count-tile pushes with typed data and one loop; preserve labels, order, nullable values and tones; confirm group 0bfbb1bf disappears.
- [x] **D3 — sync type duplication** (delegated writer): remove clone group d322e06e with minimal type-only factoring, retaining explicit camelCase/wire shapes and optional patch keys; confirm exact group and no new introduced clone, typecheck and contract tests.
- [x] **D4 — independent final verification** (parent): inspect diff and check file caps, focused/full gates, current dharness on staged files, changed-scope React Doctor and Fallow audit against `dev`; record failed/skipped/baseline checks.
- [ ] **D5 — commit decision** (parent, blocked on user approval): review the 984-line staged candidate as three work units, then commit only after fresh explicit authorization. Push/PR/merge are separate decisions.

## Acceptance
Changed-scope React Doctor reports no finding in the branch; Fallow `audit --base dev` reports zero introduced clone groups and no new other findings; `dharness check` actually analyzes staged files (not the no-op case); behavior and pre-commit gates pass. The full pinned React Doctor scan's unrelated 220 findings are reported, not silently claimed fixed. Preserve all historical artifacts.

## Progress
- 2026-09-25: Read-only mapping and tool baseline completed.
- D1 committed as `eb23aba` (`fix(animes): keep chapter mutation finalizer outside the hook`), with installed pre-commit hook green (196 suites / 1691 tests). Extracted chapter mutation body into module-level `runChapterMutation` and left simpler refresh finalizer unchanged; same-anime guard, before-guard observation, telemetry switch, toast safety and lock release preserved. RED diagnostic one `react-hooks-js/todo` at line 237 and new helper test failed before implementation; GREEN focused Jest 41/41, TypeScript and targeted ESLint exit 0, changed-scope React Doctor exit 0 (cache-free rerun also exit 0). Mutations deleting finalizer, toast guard and telemetry-off forwarding failed tests then were restored. Independent verifier re-ran 41/41 and React Doctor exit 0. Caveat: changed-scope CLI did not include new untracked helper; stage exact files before final scan so the complete candidate is analyzed. No commit; full gate still pending.
- D2 implemented (uncommitted): nine Settings convergence count tiles now come from a typed descriptor table and one loop; null/zero, Spanish labels, icons, tones and order retained. RED new table cases 4 fail/4 pass; GREEN 22/22 Settings tests; deletions/tone/order/label mutations fail and were restored; TypeScript and targeted ESLint pass. Independent verifier confirmed `dup:0bfbb1bf` gone but flagged gratuitous 179–204-character rows. The writer corrected them to conventional multiline descriptors and re-ran Fallow: the clone remains absent, **no code-golf formatting required**. No commit; full gate pending.
- D3 implemented (uncommitted): status counters now have one required key set split by number vs nullable, exposed through `Readonly<Record<...>>` snapshot and `Partial` patch, while the deliberate camelCase/wire declarations remain explicit. Added compile-time `@ts-expect-error` contract tests; `npx tsc --noEmit` and focused Jest (70 existing + 2 new) pass. An independent verifier compared all 14 counter fields against HEAD: requiredness, readonly and nullability unchanged; no wire edits. Fallow `audit --base dev` now reports `duplication_introduced: 0`, `dead_code_introduced: 0`, `complexity_introduced: 0`, verdict `pass`; two complexity findings remain inherited.
- D4 verification of the staged 11-file candidate: independent `npx lefthook run pre-commit` exits 0 (full Jest, lint, typecheck, fallow audit; mutation-staged ran no mutants because these files are outside its three surfaces). `npx -y react-doctor@latest . --verbose --diff` exits 0 and now includes the staged new helper; Fallow `audit --base dev` exits 0 with clone groups 0 and all introduced counts 0. `git diff --cached --check` exits 0; all file caps pass. **`dharness check` exits 1** despite actually scanning 10 staged source files: its ESLint, staged React Doctor and Fallow audit steps pass; the final repo-wide `fallow dupes` fails on 7.9% duplication vs a 3% threshold. Its clones are exclusively `themes/*.css` and `settings-sync-status.helpers.ts`/`sync-visible-status.helpers.ts`, none changed in this candidate OR since `dev` (`git diff --name-only dev...HEAD` on those paths is empty). This is inherited global debt outside the three selected findings; never report `dharness check` as green. The pinned full React Doctor scan's 220 findings remain outside scope. No device/bridge run or build. The 984-line candidate exceeds the review heuristic, so recommend three work-unit commits after user approval; nothing has been committed or pushed.
