# Kotlin native test debt

## Objective and baseline

Pay the pre-existing Kotlin testing debt identified in `odd/tasks/native-foreground-sync-service.md` (in the parallel feature worktree), without changing or blocking that feature. This isolated worktree is `test/kotlin-native-debt` from `dev@ac18acb`, at `D:/dev/disble/autoreas-sp/autoreas-mobile-kotlin-test-debt`. The clean baseline has no Kotlin test harness or generated root `android/` project. The feature branch's uncommitted tests/harness are not imported.

## Scope and decisions

- Cover P1 and P2 pre-existing behavior only. New T1–T6 foreground-service behavior belongs to its separate feature; retired ticker code and JS debt are excluded. User authorized two narrowly scoped production bug fixes on 2026-09-23: reject wrong-typed snapshot `id`/`name` and prevent another native attempt from replacing an unexpired lease fence; each gets a real RED → GREEN test cycle.
- Follow the TDD skill: behavior through agreed observable seams, independent expected results, narrow vertical RED → GREEN cycles, no private-method/mocked-internal assertions or speculative production changes. The user's 2026-09-23 clarification removes mutation testing from this effort; record observed RED instead. This overrides the mutation-proven exit criterion of the source debt section only for this effort.
- Agreed seams (user confirmation, 2026-09-23): parser's `parse`/wire mapping; lease `claim`/`release` and fenced database effects; response applier transaction outcome; recovery's journal/database effects; cycle's externally observable HTTP/database/journal result. Avoid runner internals unless its existing watchdog/abandon/crash behavior is observable only there. Tests of existing correct behavior may start GREEN; never alter production merely to manufacture RED. Capture an actual RED when a test reveals a real defect or when adding new behavior.
- Use host JUnit 4 / Robolectric plus a bounded loopback `ServerSocket` fixture for HTTP integration. Generate the ignored Expo `android/` project inside this worktree only. `@SQLiteMode(NATIVE)` proves host Robolectric SQLite UPSERT behavior; Android-device/API compatibility remains unverified.
- P3 is best effort after P1/P2; decide retire versus unify `SyncJournalModule` before testing it. P4 `postJson` is covered indirectly by cycle integration.
- User authorized a bounded expansion on 2026-09-23 to make the failing Fallow pre-commit gate pass in this same isolated worktree; do not disable/suppress the gate or change unrelated TS/TSX. Fallow JSON reports three introduced complexity findings against stale `origin/main` base `cd2aae7e` (228 changed files): `resolveTransientEntry`, test helper `runAsync`, and `getAnimeCover`. Two inherited complexity findings and CSS drift advisories are not blockers. Resolve with behavior-preserving tests/refactors, not a policy bypass.
- Repository `AGENTS.md` requires explicit user confirmation after validation and diff review. The user granted it for local work-unit commits on 2026-09-23; never stage/commit existing changes from another worktree. No push or PR is authorized.

## Delivery and verification

TDD source: project `AGENTS.md` (RED → GREEN → MUTATE → REFACTOR for hooks/helpers), source debt plan and user's explicit RED → GREEN-only clarification; proven host runner: `./gradlew :sync-engine:testDebugUnitTest --console=plain` from generated `android/`, freshly rerun with 43 tests/0 failures. Focused test commands and final `npx lefthook run pre-commit` must be observed; report failures, skipped, and environmental blocks. Forecast: more than 400 authored added/deleted lines of tests + fixtures across P1/P2; delivery strategy `ask-on-risk`. After final validation and diff review, user explicitly authorized local work-unit commits and chose future **PRs stacked toward main** (2026-09-23); no push or PR was authorized. Planned slices (review budget ~400 authored changed lines each): (1) Fallow TS refactors/tests ~132; (2) harness + parser/lease fixes/tests ~336; (3) applier tests 224; (4) recovery tests + shared DB fixture 223; (5) cycle success + HTTP fixture ≤400; (6) cycle failure/reclaim cases ≤400; (7) P3 request body + this task document ~140. The cycle file must be split by test-method groups, never by omitting tests. Confirm exact staged diffs and gate at each commit. No production behavior changes are authorized merely to make tests pass.

## Tasks

- [x] **K1 — Bootstrap isolated host test harness.** JUnit/Robolectric configured and parser tracer added; focused Gradle runner passed (1 test, 0 failures). Included in local commit `8fb3779` with parser/lease tests; pre-commit green. Route: delegated writer. Checks: passing and committed locally.
- [x] **K2 — P1 parser and wire mapping.** Added 16 characterization tests plus two genuine RED cases for wrong-typed snapshot `id`/`name` (18 tests, 2 failures before code fix). Narrow `requireString` fix made the focused suite GREEN (18/0); full module suite passed 24/0. Independent verifier flagged an initially incomplete negative fixture; corrected it with required `modified_at:0` and reran (18/0), now only the targeted type differs from the wire contract. Local commit `8fb3779`; fresh focused Gradle green. Route: delegated writer. Checks: RED → GREEN confirmed, committed locally.
- [x] **K3 — P1 lease and fence.** Genuine RED 4 tests/1 failure preceded narrow lease UPSERT fix. Focused suite now passes 6 tests/0 failures (fresh rerun; independently verified); full module suite passed 22/0. Test-side synthetic guarded SQL was removed; K6 covers the real cycle's lost-lease boundary, not each private guarded SQL statement independently. `@SQLiteMode(NATIVE)` supports the UPSERT on the host (device behavior unverified). Local commit `8fb3779`; fresh focused Gradle green. Route: delegated writer. Checks: lease GREEN, committed locally.
- [x] **K4 — P2 response applier.** Five real SQLite tests now pass: statuses including unsupported-operation dead letter, normalized staging, cursor advance/non-regression, zero-valued and last-wins tokens, and transaction rollback of preceding writes. Initial worker timed out after creating three tests and its incomplete fixture failed (3/1); incident review found missing rejection input, corrected test to 3/0, then added two tests for 5/0. Independent verifier and parent fresh rerun passed 5/0. Cursor assertion after an earlier injected failure does not prove rollback of a cursor write; cycle's actual fenced status writer remains K6. Local commit `cebfd5e` (224 additions); focused Gradle and pre-commit green. Route: delegated writer. Checks: focused GREEN 5/0, committed locally.
- [x] **K5 — P2 recovery.** Five real SQLite/journal tests cover stale compensation, orphan processing rows, fresh/terminal exclusion and lost lease. Independent verifier found shared journal contamination and a too-small time margin; corrected with a short, isolated UUID temp directory per fixture and current timestamp. Fresh focused rerun 5/0, full module suite reported 34/0; independently verified and parent spot-checked. Local commit `aeb2915` includes shared DB fixture; focused Gradle and pre-commit green. Route: delegated writer. Checks: passing characterization, no artificial RED; committed locally.
- [x] **K6 — P2 cycle attempt and moved pre-existing runner behavior.** Six cycle integration tests pass: real HTTP success (synced/cursor/journal), 4xx dead-letter, 5xx retry/pending, malformed 2xx retry, closed-port transport retry, and mid-request real lease reclaim (abandoned without applying response; successor fence and persisted journal retained). The request server has bounded input/time and cleanup; journal fixture uses a short UUID temp directory with allowlisted nonrecursive cleanup after diagnosing `SQLITE_CANTOPEN` on Windows. Full module suite freshly passes 40/0 (independent verifier). The lost-lease scenario proves transaction-boundary rejection, not each private SQL guard independently. Watchdog/runner mapping only exists in parallel uncommitted feature and cannot be tested from this baseline; reconcile after that feature is integrated. Local success-fixture commit `d7d3778` (287 additions) followed by failed/reclaimed-case commit `342c52b` (174 additions); both focused Gradle and pre-commit green. Route: delegated writer. Checks: focused cycle 6/0, prior full Gradle 40/0; committed locally.
- [x] **K8 — P3 request body best effort.** Three direct JUnit tests characterize absent/null versus zero base token, update-only alias normalization/precedence, and malformed-syntax fallback via `ReconcileRequestBody.build`. Focused test 3/0, full module suite 43/0; independent verifier confirmed these cases but notes non-object JSON and another alias remain outside this best-effort slice. This test and task record share the final local work-unit commit. Route: delegated writer. Checks: focused GREEN 3/0, committed locally.
- [x] **K9 — Resolve introduced Fallow complexity gate.** Behavior-preserving refactors removed the three introduced findings (`resolveTransientEntry`, fake-store `runAsync`, `getAnimeCover`) without suppression/config changes. Focused Jest passed 38/38, 11/11, 28/28 and scoped ESLint passed. Parent-owned TS mutation checks after staging green code observed 3 cover-helper failures and 4 cover-header failures; both restored from INDEX and reran GREEN. Fallow JSON verdict now `pass`, 0 introduced complexity findings (2 inherited). First local work-unit commit `9770da6` contains all four TS paths (~132 changed lines); its pre-commit hook passed. Route: delegated mapping/writers plus parent mutation checks. Checks: targeted green and audit pass.
- [x] **K7 — Close debt scope and full validation.** Keep `SyncJournalModule` for the active JS fallback; unify with `SyncEngineJournal` in separate native-engine T5 work and keep shared schema/limits aligned meanwhile. Parent full Gradle passed 43/0, independent expanded staged-diff review found no concrete defect, `git diff --cached --check` passed. After K9, `npx lefthook run pre-commit` PASSED: Fallow, staged ESLint, typecheck, and Jest (185 suites, 1418 tests); the TS staged-mutation guard ran but mutations were skipped because none of its three configured surfaces were staged. No Kotlin mutation tests by user decision. User approved local work-unit commits after validation and review; final doc and request-body tests close the series. Route: delegated verification plus parent decision. Checks: P1/P2 GREEN, journal decision recorded, gate GREEN; local commits only.

## Progress and next step

- 2026-09-23: Established clean isolated worktree from `dev@ac18acb`, registered it with session coordination. Mapping confirmed no baseline Kotlin test harness or generated root Android project. No Kotlin source/tests edited; existing parallel branch remains untouched.
- 2026-09-23: After the final green gate and independent diff review, user explicitly authorized local work-unit commits and selected future PRs stacked toward main; no push/PR. K1–K6 plus K8 Kotlin debt implemented; parent fresh Gradle 43/0. K9 cleared three introduced Fallow findings with behavior-preserving TS refactors and manual mutation checks (3/4 expected failures, restored from index), no suppressions/policy changes. Fallow JSON `pass` (0 introduced, 2 inherited). Final staged hook PASSED: audit, ESLint, typecheck, Jest 185 suites/1418 tests; TS mutation guard ran but execution skipped (no configured surface staged). Independent expanded staged-diff review found no concrete defect; 16 paths, 1471 additions/43 deletions before this final doc update, all ≤500 lines. Native assessment unassessable (schema-incompatible), independent verifiers used. No Kotlin mutation tests per user. Seven local work units: Fallow `9770da6`, harness/parser/lease `8fb3779`, applier `cebfd5e`, recovery `aeb2915`, cycle success `d7d3778`, cycle failures/reclaim `342c52b`, and P3/documentation in the final commit. No push/PR; K6 Runner watchdog from the parallel feature remains a postintegration follow-up.
